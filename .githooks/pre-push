#!/bin/sh
# Run checks before push
set -e

echo "Running pre-push checks..."

# Verify Swift source compiles (syntax check)
echo "→ Checking Swift syntax..."
swiftc -parse app/main.swift 2>/dev/null || echo "  (swiftc not available, skipping)"

echo "✓ All pre-push checks passed"
